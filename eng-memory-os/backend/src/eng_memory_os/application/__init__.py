"""
Application layer: use cases, event handlers, and pipeline orchestration.

This layer orchestrates business workflows by coordinating domain entities
and infrastructure services. It contains no business logic itself —
that lives in the domain layer.
"""
